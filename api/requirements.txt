# Pinned exactly, not as ranges, for two reasons: CI then tests the same
# versions production runs, and the versions running in production are recorded
# here in the repo. Deploys install this file into the app's virtualenv on the
# host (see docs/hosting.md), so what is pinned here is what ships.
#
# Watch the interpreter floor: google-auth 2.51+ requires Python >= 3.10. The
# host venvs and CI both run 3.11, so a bump that raises the floor is fine —
# but check Requires-Python before pinning past a major dependency jump.
Flask==3.1.3
google-api-python-client==2.199.0
google-auth==2.56.3
# Both arrive anyway as google-api-python-client dependencies, but
# services/sheets_service.py imports them directly to build the HTTP transport
# with an explicit timeout — `build(credentials=...)` makes its own with none.
# A direct import wants a direct pin: otherwise a google-api-python-client bump
# that drops or reshuffles them breaks the Sheets write with an ImportError
# that names a package nothing in the repo appears to depend on.
google-auth-httplib2==0.4.1
httplib2==0.32.0
# APM agent, initialised in passenger_wsgi.py before the app is imported. It
# ships cp311 manylinux wheels, so the host installs a wheel rather than
# compiling — check that still holds before bumping, because the cPanel venv has
# no toolchain to fall back on. Inert without NEW_RELIC_LICENSE_KEY, so
# installing it locally or in CI costs nothing at runtime.
newrelic==13.5.0
