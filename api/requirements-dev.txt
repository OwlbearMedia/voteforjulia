# Development and CI tooling. Deliberately separate from requirements.txt:
# that file is installed into the host's cPanel virtualenv on every deploy
# (see docs/hosting.md), so anything added there ships to production. Nothing
# in this file is imported by the app at runtime.
#
# Pinned like requirements.txt so CI results are reproducible.
-r requirements.txt

coverage==7.15.2
openapi-spec-validator==0.9.0
pytest==9.1.1
pytest-cov==7.1.0
PyYAML==6.0.3
ruff==0.16.1
